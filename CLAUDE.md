# 작업 규칙 (이 저장소 전용)

## Git 작업 규칙
- `git commit`은 사용자가 대화에서 명시적으로 요청할 때만 실행한다. 작업이 끝났다고 스스로 판단해서 임의로 커밋하지 않는다.
- `git push`는 절대 자동으로 하지 않는다. 커밋과 별개로 항상 먼저 "푸시할까요?"라고 확인받은 뒤에만 실행한다.
- 한 번의 커밋/푸시 승인은 그 순간에만 유효하다. 다음 변경 건에도 이어서 자동 적용하지 않는다.
- 커밋 메시지는 무엇을 왜 고쳤는지 한국어로 간결하게 작성한다.

## 브랜치/폴더 원칙
- 운영(work, production) 브랜치·폴더는 절대 직접 수정하지 않는다. 반드시 스테이징(dev) 브랜치에서 먼저 작업하고 검증한다.
- 운영 폴더로의 반영(수동 복사 또는 merge)은 사용자가 직접 하거나 명시적으로 지시할 때만 진행한다.

## 수정 범위 원칙
- 이미 완성되어 정상 동작 중인 기능은 요청받지 않는 한 건드리지 않는다. 신규 기능 추가나 명시적으로 지목된 버그만 수정한다.
- 여러 파일에 걸친 수정을 할 때는 관련 없는 파일까지 임의로 리팩토링하지 않는다.

## 오류 대응 원칙
- 수정한 코드에 문제가 있으면 즉시 인정하고 원인을 설명한 뒤 바로 고친다. 변명하거나 책임을 돌리지 않는다.
- 확신이 서지 않는 부분은 추측으로 진행하지 말고, 먼저 사용자에게 방향을 확인한다.

## 착수 전 확인
- 요청 범위가 모호하거나 여러 방식으로 해석될 수 있으면, 작업 시작 전에 어떤 방식으로 진행할지 먼저 확인한다.
- 요청받지 않은 추가 작업(예상치 못한 리팩토링, 파일 구조 변경 등)은 임의로 하지 않는다.
